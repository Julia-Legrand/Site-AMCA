{% extends 'base.html.twig' %}

{% block title %}Modifier la photo
{% endblock %}

{% block body %}
	{% include "_partials/_headerAdmin.html.twig" %}
	<section class="row m-0">
		{% include "_partials/_sidebar.html.twig" %}

		<div class="dashboard editForm" style="height:100vh;">
			<h1>Modifier la photo</h1>

			{{ include('trip_pictures/_form.html.twig') }}
			<div class="bloc">
				<button class="button" id="customSubmitButton">Valider</button>
				{{ include('trip_pictures/_delete_form.html.twig') }}
				<a class="button" href="{{ path('app_trip_pictures_index') }}">Retour</a>
			</div>
		</div>
	</section>

	<script>
		document.getElementById('customSubmitButton').addEventListener('click', function () {
document.querySelector('.submit-button').click();
});
	</script>
{% endblock %}
