<header class="headerAdmin">
	<nav class="navbar navbar-expand-md navbar-dark bg-dark d-flex justify-content-between">
		<div class="bloc-logo">
			<a href="{{ path('home') }}">
				<img src="{{ asset('images/logo-noir-transparent.png') }}" alt="logo AMCA">
			</a>
		</div>
		<div class="bloc-icon">
			<ul class="navbar-nav-admin my-auto">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_logout') }}">
							<i class="fa-solid fa-right-from-bracket" style="color:#A4A5A7;"></i>
						</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</nav>
</header>
