<form method="post" action="{{ path('app_future_trips_delete', {'id': future_trip.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ future_trip.id) }}">
    <button class="btn">Supprimer</button>
</form>
