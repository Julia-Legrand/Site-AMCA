<form method="post" action="{{ path('app_memberships_delete', {'id': membership.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membership.id) }}">
    <button class="btn">Supprimer</button>
</form>
