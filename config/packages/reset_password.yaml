symfonycasts_reset_password:
  request_password_repository: App\Repository\ResetPasswordRequestRepository

  #   Length of time a reset password request is valid for in seconds after it has been created
  lifetime: 3600

  # Enable the Reset Password Cleaner which handles expired reset password requests that may have been left in persistence
  enable_garbage_collection: true
