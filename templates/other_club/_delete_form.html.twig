<form method="post" action="{{ path('app_other_club_delete', {'id': other_club.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ other_club.id) }}">
	<button class="button">Supprimer</button>
</form>
