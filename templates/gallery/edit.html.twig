{% extends 'base.html.twig' %}

{% block title %}Modifier la photo{% endblock %}

{% block body %}
{% include "_partials/_headerAdmin.html.twig" %}
	<section>
		{% include "_partials/_sidebar.html.twig" %}

		<div class="dashboard editForm">
            <h1>Modifier la photo</h1>

            {{ include('gallery/_form.html.twig') }}
            <div class="bloc">
				<button class="button" id="customSubmitButton">Valider</button>
                 {{ include('gallery/_delete_form.html.twig') }}
                 <a class="button" href="{{ path('app_gallery_index') }}">Retour</a>
			</div>
		</div>
	</section>

	<script src="{{ asset('js/scripts.js') }}"></script>
{% endblock %}