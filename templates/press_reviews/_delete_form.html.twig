<form method="post" action="{{ path('app_press_reviews_delete', {'id': press_review.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ press_review.id) }}">
	<button class="button">Supprimer</button>
</form>
